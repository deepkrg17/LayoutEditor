<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/undo"
        android:name="@string/undo"
        android:title="@string/undo"
        android:icon="@drawable/undo"
        app:contentDescription="@string/undo"
        app:showAsAction="always" />
    <item
        android:id="@+id/redo"
        android:name="@string/redo"
        android:title="@string/redo"
        android:icon="@drawable/redo"
        app:contentDescription="@string/redo"
        app:showAsAction="always" />

    <item
        android:id="@+id/component_tree"
        android:name="@string/component_tree"
        android:title="@string/component_tree"
        android:icon="@drawable/file_tree"
        app:contentDescription="@string/component_tree"
        app:showAsAction="always" />

</menu>
