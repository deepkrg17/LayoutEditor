<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style
        name="Base.AppTheme"
        parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:fontFamily">@font/rubik</item>
    </style>

    <style
        name="AppTheme"
        parent="Base.AppTheme">
        <item name="materialAlertDialogTheme">@style/MaterialAlertDialog.Monet</item>
        <item name="alertDialogTheme">@style/ThemeOverlay.Material3.MaterialAlertDialog</item>
        <item name="dialogCornerRadius">28dp</item>
        <item name="switchPreferenceCompatStyle">@style/AppTheme.SwitchPreference</item>

        <item name="textAppearanceLargePopupMenu">@style/TextAppearance.App.BodyLarge</item>
        <item name="textAppearanceSmallPopupMenu">@style/TextAppearance.App.BodyLarge</item>
        <item name="popupMenuBackground">@drawable/custom_popup_background</item>
        <item name="android:popupElevation">0dp</item>
    </style>

    <style
        name="TextAppearance.App.BodyLarge"
        parent="TextAppearance.Material3.BodyLarge">
        <item name="fontFamily">@font/rubik</item>
        <item name="android:fontFamily">@font/rubik</item>
        <item name="android:textColor">?attr/colorOnPrimaryContainer</item>
    </style>
    <style
        name="MaterialAlertDialog.Monet"
        parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="dialogCornerRadius">28dp</item>
        <item name="android:background">@drawable/dialog_shape</item>
    </style>

    <style
        name="AppTheme.SwitchPreference"
        parent="Preference.SwitchPreference.Material">
        <item name="android:widgetLayout">@layout/layout_material_switch</item>
    </style>

    <style
        name="AppTheme.Licence"
        parent="AppTheme">
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="ToolbarButtonStyle">
        <item name="android:layout_width">35dp</item>
        <item name="android:layout_height">35dp</item>
        <item name="android:padding">5dp</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>
</resources>
